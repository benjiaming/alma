import React from 'react';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import '@testing-library/jest-dom';
import PublicLeadForm from './PublicLeadForm';

describe('PublicLeadForm', () => {
    test('will pass when the test is correct', () => {
        expect(true).toBe(true);
    });

//     test('renders the form', () => {
//         render(<PublicLeadForm />);
//         expect(screen.getByText('Want to understand your visa options?')).toBeInTheDocument();
//     });

})
